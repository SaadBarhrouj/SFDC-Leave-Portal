<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Leave_Request_Single_Year</fullName>
    <active>true</active>
    <description>Prevents leave requests from spanning two different years, since balances are managed per year.</description>
    <errorConditionFormula>YEAR( Start_Date__c )  &lt;&gt;  YEAR( End_Date__c )</errorConditionFormula>
    <errorMessage>Prevents leave requests from spanning two different years, since balances are managed per year.</errorMessage>
</ValidationRule>
